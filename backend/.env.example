# Copy this file to .env and adjust as needed. No API keys are required for this project.

# Port the Express server listens on
PORT=4000

# Comma-free single origin allowed to call the API in production (use '*' only for local/demo use)
CORS_ORIGIN=*
